.pagination {
    @apply flex justify-between mt-4 mb-4 text-xl;
}
.pagination [data-direction] {
    @apply flex items-center justify-between hover:underline;
}
.pagination [data-direction='forwards'] {
    @apply ml-auto;
}
.pagination [data-direction='backwards'] {
    @apply mr-auto;
}
